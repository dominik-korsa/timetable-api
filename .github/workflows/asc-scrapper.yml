name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Install modules
      run: pnpm install
    - name: Run tests
      run: pnpm exec nx run @timetable-api/asc-scrapper:test
